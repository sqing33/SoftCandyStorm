# Demo Buildcraft Repair v13 Candidates

本候选批次只修复 v12 剩余的 Coward / Greedy 双低问题。

v13 基于 repair v12 full pack，仍只作为 generated candidate。调整重点：

- 只覆盖 `sprinkle-spitter`。
- 恢复 `sprinkle-spitter` 的 XP / score 到 v11 能救回 Coward 的数值。
- 恢复 `sprinkle-spitter` 的冷却、持续时间和散布到 v10 能保留 Greedy 路线的节奏。
- 不改 `frosting-grassland-standard` 的 v10 波次结构，单轴验证远程怪自身节奏。
- 不改玩家武器、被动、Boss、地图或正式内容池。

状态：generated candidate only，不能直接进入 accepted_content 或 Runtime 正式内容。
