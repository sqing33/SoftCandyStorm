# Demo Buildcraft Repair v5 Candidates

本候选批次只修复 `frosting-grassland-standard` 的 Boss 后 210-300 秒压力。

v5 基于 repair v4 full pack，不再继续削弱玩家武器。调整重点是把少量 `sprinkle-spitter` 提前到 210-300 秒，让 Greedy / Kite 在 Boss 后阶段承受可读的远程压力，同时保留 Idle、Random、Coward、Tank、BossHunter、ZoneControl 和 Route 的目标区间。

状态：generated candidate only，不能直接进入 accepted_content 或 Runtime 正式内容。
