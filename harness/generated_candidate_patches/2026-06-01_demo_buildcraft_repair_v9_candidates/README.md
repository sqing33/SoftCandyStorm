# Demo Buildcraft Repair v9 Candidates

本候选批次窄修 v8 在 300 秒 / 3 seed 烟测中剩下的 Greedy / Route 偏高问题。

v9 基于 repair v8 full pack，仍只作为 generated candidate。调整重点：

- 保留 `bubble-shoes` 的恢复值，继续保护移动手感。
- 维持 `sprinkle-spitter` 低 XP，并把 210-300 秒远程压力作为不喂经济的可读挑战。
- 继续沿用 v8 的焦糖控制和波次节奏，不增加大范围压力。
- 进一步降低 `candy-crystal-lens` 的 XP 乘区，压住 Greedy 的滚雪球。
- 小幅降低 `star-spoon` 拾取半径，减少 Route 固定路径的安全收益。

状态：generated candidate only，不能直接进入 accepted_content 或 Runtime 正式内容。
