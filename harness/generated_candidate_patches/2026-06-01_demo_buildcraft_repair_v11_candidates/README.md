# Demo Buildcraft Repair v11 Candidates

本候选批次只修复 v10 剩余的 Coward 偏低问题。

v11 基于 repair v10 full pack，仍只作为 generated candidate。调整重点：

- 只覆盖 `sprinkle-spitter`。
- 将 `sprinkle-spitter` 的 XP、冷却、持续时间和散布恢复到 v5 通过小矩阵时的可读恢复形态。
- 不改 `frosting-grassland-standard` 的 v10 波次结构，保留 Greedy / Route 已回到目标区间的结果基础。
- 不改玩家武器、被动、Boss、地图或正式内容池。

状态：generated candidate only，不能直接进入 accepted_content 或 Runtime 正式内容。
