# Demo Buildcraft Repair v12 Candidates

本候选批次只修复 v11 剩余的 Greedy 偏低问题。

v12 基于 repair v11 full pack，仍只作为 generated candidate。调整重点：

- 只覆盖 `sprinkle-spitter`。
- 将 `sprinkle-spitter` 的 XP 从 v11 的 5 下调到 4，保留 v11 的可读弹道参数。
- 不改 `frosting-grassland-standard` 的 v10 波次结构，目标是保留 Coward 恢复空间，同时让 Greedy 回到目标区间。
- 不改玩家武器、被动、Boss、地图或正式内容池。

状态：generated candidate only，不能直接进入 accepted_content 或 Runtime 正式内容。
