# 2026-05-26 mmx Runtime 俯视角与音频候选初步审查

## 总结

- 门禁结论：`needs_review`
- 候选批次：`2026-05-26_mmx_runtime_topdown_audio_plan`
- 审查范围：玩家俯视角 sprite、Boss 出场中文语音、Boss 出场音乐 sting
- 当前状态：全部仍为候选素材，不进入正式 `assets/`，不接入 Runtime

## 通过项

- 已按生成前计划把 `mmx image generate`、`mmx speech synthesize` 和 `mmx music generate` 的产物保存到 `asset/generated_candidates/`。
- 已记录 prompt、命令、格式、尺寸、时长、后处理和候选池规则。
- 图像原图为 1024x1024，并生成了透明 PNG、128/64/32 小尺寸 runtime-size 候选和棋盘格预览。
- Boss 出场语音长度约 3.24 秒，可作为事件提示候选的听感审查起点。
- Boss 出场音乐源约 72.9 秒，已裁剪出 8 秒 sting 候选。
- 已生成 `boss_arrival_voice_sting_mix_v001` 叠放预览候选，并记录 voice、sting 和 mix 的 loudnorm 探测值。

## 未通过项

- 图像仍需真人判断是否真正符合严格俯视角；`v005_002` 经过 component cleanup 后保留了最大组件，可能裁掉了边缘元素，需要人工看图确认。
- 计划中的 `Chinese_female_news_anchor` 声线在当前 `mmx speech voices` 中不存在，实际使用 `Chinese (Mandarin)_News_Anchor`，需要听感审查确认是否合适。
- Boss 音乐源长度远超 8 秒，原始源不可直接接入 Boss 出场事件。
- 8 秒剪辑仍未经过人工试听、响度统一验收、字幕/无障碍替代和与语音叠放检查。
- 叠放预览只是技术预览，尚未人工确认语音是否被音乐遮挡，也未做 Runtime ducking 或字幕时序。

## 单项结论

| 候选 | 结论 | 原因 |
|---|---|---|
| `player_jar_keeper_topdown_v005_001` | `needs_visual_review` | 小尺寸预览已生成，但需要人工检查俯视角、边缘和伪文字 |
| `player_jar_keeper_topdown_v005_002` | `repair_review` | 清理后组件更紧，但可能裁掉边缘元素，需要人工复核 |
| `voice_boss_arrival_cn_v002` | `needs_listening_review` | 声线使用了实际可用 voice 替代计划 voice，需要听感确认 |
| `sting_boss_arrival_v002_source` | `repair_trim` | 源文件约 72.9 秒，不适合作为短提示音直接接入 |
| `sting_boss_arrival_v002_trim8s` | `needs_listening_review` | 长度合格，但需要人工听感、响度和语音叠放审查 |
| `boss_arrival_voice_sting_mix_v001` | `needs_listening_review` | 已生成叠放预览，但需要人工确认语音清晰度和混音比例 |

## 下一步

- 真人检查 `review_previews/topdown_v005/contact_sheet_32.png`、`contact_sheet_64.png` 和 `contact_sheet_128.png`。
- 真人试听 `voice_boss_arrival_cn_v002.mp3`、`sting_boss_arrival_v002_trim8s.mp3` 和 `boss_arrival_voice_sting_mix_v001.mp3`，记录具体听感、响度、遮挡和触发风险。
- 若真人审查认可，可填写 `harness/asset_review/drafts/2026-05-26_mmx_runtime_topdown_audio_plan_review_draft.json` 后运行人工审查校验器。
